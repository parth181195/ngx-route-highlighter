import { RouteHighlighterDirective } from './route-highlighter.directive';

describe('RouteHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new RouteHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
